# Clipper — Chrome Extension

Chrome-расширение для извлечения контента веб-страниц и конвертации в Markdown. Отсекает рекламу, навигацию, футеры и прочий мусор, оставляя только полезное содержимое.

## Как это работает

Расширение использует [Mozilla Readability](https://github.com/mozilla/readability) — тот же алгоритм, что работает в режиме Reader View в Firefox. Он анализирует структуру страницы и определяет, что является основным контентом, а что — обвязкой.

Далее [Turndown](https://github.com/mixmark-io/turndown) конвертирует очищенный HTML в чистый Markdown.

Для x.com (Twitter) применяется отдельный экстрактор, который собирает текст твитов напрямую из DOM, поскольку стандартный алгоритм Readability не справляется со структурой твиттера.

Content script загружается **только по клику** на иконку расширения (lazy injection), а не на каждой открытой вкладке.

## Возможности

- Извлечение контента любой веб-страницы в Markdown одним кликом
- YAML frontmatter (title, source, created, tags)
- Автоматическое удаление рекламы, навигации, футеров, сайдбаров, cookie-баннеров
- Сохранение результата в `.md` файл или копирование в буфер обмена
- Поддержка x.com / Twitter — извлечение твитов, тредов, ленты
- Fallback для SPA-виджетов, каталогов, комментариев
- GitHub Flavored Markdown — таблицы, зачёркивания, списки задач
- Quote-твиты извлекаются как цитаты

## Установка

1. Клонируйте репозиторий
2. Откройте `chrome://extensions` в Chrome
3. Включите **Режим разработчика**
4. Нажмите **Загрузить распакованное расширение**
5. Выберите папку репозитория

`dist/` уже содержит собранные файлы — `npm run build` не обязателен для использования.

## Разработка

```bash
npm install
npm run build    # сборка dist/content.js + dist/popup.js
npm run watch    # watch-режим
```

После изменений в `src/` выполните `npm run build` и перезагрузите расширение в `chrome://extensions`.

## Использование

1. Откройте веб-страницу или x.com
2. Нажмите иконку **Clipper**
3. Дождитесь статуса «Готово: {заголовок}»
4. **Скачать .md** или **Копировать в буфер**

## Структура проекта

```
├── manifest.json
├── popup.html
├── dist/
│   ├── content.js      # собранный content script
│   └── popup.js        # собранный popup
├── src/
│   ├── clipper/        # логика извлечения
│   └── popup/          # исходник popup
├── build.mjs           # esbuild
├── package.json
├── tests/
│   ├── fixtures-urls.txt
│   └── baseline/
└── icons/
```

## Разрешения

| Разрешение | Зачем |
|------------|-------|
| `activeTab` | Доступ к активной вкладке |
| `scripting` | Инжект content script по клику |
| `downloads` | Скачивание `.md` файла |
| `clipboardWrite` | Копирование в буфер обмена |

## Лицензия

Apache 2.0